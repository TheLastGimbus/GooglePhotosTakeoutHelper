name: gpth
description: 'Tool to help you with exporting stuff from Google Photos'
version: 3.4.3
homepage: 'https://github.com/TheLastGimbus/GooglePhotosTakeoutHelper'
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  args: ^2.4.2      # CLI argument parsing
  collection: ^1.18.0
  console_bars: ^1.2.0
  convert: ^3.1.1
  crypto: ^3.0.3    # For SHA-256
  exif: ^3.1.4
  file_picker_desktop: ^1.1.1
  fuzzysearch: ^0.1.3
  image: ^4.0.17    # For EXIF in HEIC/RAW
  logging: ^1.1.1   # Structured logging
  mime: ^1.0.4
  path: ^1.8.3
  proper_filesize: ^0.0.2
  unorm_dart: ^0.2.0
  # archive:
  #   git:
  #     url: https://github.com/TheLastGimbus/archive.git
  #     ref: fix-windoza-extract-errors

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.6
